# Ruff configuration for the tests/ helper scripts and their unit tests.
# See CONTRIBUTING.md for how to run this locally.
target-version = "py312"
line-length = 100

[lint]
select = ["E4", "E7", "E9", "F"]
